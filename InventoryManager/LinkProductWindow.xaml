<Window x:Class="InventoryManager.LinkProductWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kelola Tautan Produk" Height="500" Width="850"
        WindowStartupLocation="CenterOwner"
        Background="#F0F2F5">
    <Window.Resources>
        <Style TargetType="Border" x:Key="MainBorderStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="HeaderTextStyle">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#A0A0A0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="Button" x:Key="CloseButtonStyle" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#6c757d"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5a6268"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,15" Foreground="#1E1E1E"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Panel Kiri: Daftar Produk untuk Ditautkan -->
            <Border Grid.Column="0" Style="{StaticResource MainBorderStyle}" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Produk Tersedia:" Style="{StaticResource HeaderTextStyle}" Margin="5,0,0,10"/>
                    <ListView x:Name="AvailableProductsListView" Grid.Row="1" ItemsSource="{Binding AvailableProducts}" DisplayMemberPath="ProductName" SelectionChanged="AvailableProductsListView_SelectionChanged"/>
                </Grid>
            </Border>

            <!-- Tombol Aksi di Tengah -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="15,0">
                <Button x:Name="AddLinkButton" Content=" &gt;&gt; " Click="AddLinkButton_Click" ToolTip="Tautkan Produk Terpilih" Style="{StaticResource ActionButtonStyle}" IsEnabled="False"/>
                <Button x:Name="RemoveLinkButton" Content=" &lt;&lt; " Margin="0,10,0,0" Click="RemoveLinkButton_Click" ToolTip="Hapus Tautan Produk Terpilih" Style="{StaticResource ActionButtonStyle}" IsEnabled="False"/>
            </StackPanel>

            <!-- Panel Kanan: Daftar Produk yang Sudah Ditautkan -->
            <Border Grid.Column="2" Style="{StaticResource MainBorderStyle}" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Produk Tertaut:" Style="{StaticResource HeaderTextStyle}" Margin="5,0,0,10"/>
                    <ListView x:Name="LinkedProductsListView" Grid.Row="1" ItemsSource="{Binding LinkedProducts}" DisplayMemberPath="ProductName" SelectionChanged="LinkedProductsListView_SelectionChanged"/>
                </Grid>
            </Border>
        </Grid>

        <!-- Tombol Tutup -->
        <Button Grid.Row="2" Grid.Column="2" Content="Selesai" HorizontalAlignment="Right" VerticalAlignment="Bottom" Click="CloseButton_Click" Style="{StaticResource CloseButtonStyle}" Margin="0,15,0,0"/>
    </Grid>
</Window>
